PASS 1 — EVIDENCE FORM GENERATION

You are generating a tailored evidence-collection form for a user who is returning or
selling a used item. Based on the return/sell reason, the initial photos, and the
product listing data, decide exactly what additional evidence (photos and fields) is
needed to fairly grade this specific item and verify the stated reason.

INPUT CONTEXT:
- Reason: {reason}
- Category: {category}
- Product listing data: {listing_data}

Produce a Form_Schema as a JSON object with this exact shape:
{{
  "title": "<short form title>",
  "fields": [
    {{
      "id": "<snake_case_field_id>",
      "label": "<human readable label>",
      "type": "photo | text | boolean | select",
      "required": true,
      "guidance": "<what the user should capture or enter>",
      "options": ["<only for type=select>"]
    }}
  ],
  "photo_guidance": ["<short tip>", "<short tip>"]
}}

Tailor fields to the reason and category. Always include at least one photo field for
any visible defect the user mentions. Return ONLY the JSON object.
